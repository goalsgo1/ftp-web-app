# 🔑 공개키 등록 - 간단한 방법

현재 문제:
- `deploy_key.pub` 파일이 리눅스 서버에 없음 (윈도우에 있음)
- 같은 서버에 SSH 접속 시도 (불필요함)

**더 간단한 방법으로 진행하세요!**

---

## ✅ 가장 간단한 방법

### 방법 1: 파일 내용을 직접 입력 (권장) ⭐

#### Step 1: 윈도우에서 공개키 내용 확인

**PowerShell에서:**

```powershell
cd C:\FTP
Get-Content deploy_key.pub
```

**공개키 내용 예시:**
```
ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIFwbll+D//Sc6TaC+cLPX7PfOA59iDDed/8f9FJpold6 deploy@server
```

#### Step 2: 리눅스 서버에서 직접 입력

**리눅스 서버에서:**

```bash
# SSH 디렉토리 생성
sudo mkdir -p /home/deploy/.ssh
sudo chmod 700 /home/deploy/.ssh

# authorized_keys 파일 생성
sudo nano /home/deploy/.ssh/authorized_keys
```

**nano 에디터에서:**
1. 윈도우에서 확인한 공개키 내용을 **직접 타이핑**
2. 또는 VMware 클립보드 공유 기능 사용
3. Ctrl+X로 저장 종료
4. Y로 저장 확인
5. Enter로 파일명 확인

**권한 설정:**
```bash
sudo chown -R deploy:deploy /home/deploy/.ssh
sudo chmod 600 /home/deploy/.ssh/authorized_keys
```

---

### 방법 2: 파일을 서버로 전송

#### Step 1: 윈도우에서 파일 내용 확인

**PowerShell에서:**

```powershell
cd C:\FTP
Get-Content deploy_key.pub
```

#### Step 2: 리눅스 서버에서 파일 생성

**리눅스 서버에서:**

```bash
# 파일 생성
nano ~/deploy_key.pub
```

**nano 에디터에서:**
1. 공개키 내용 붙여넣기 (마우스 우클릭 → Paste)
2. Ctrl+X로 저장

#### Step 3: authorized_keys에 추가

```bash
# SSH 디렉토리 생성
sudo mkdir -p /home/deploy/.ssh
sudo chmod 700 /home/deploy/.ssh

# 공개키를 authorized_keys에 추가
sudo cat ~/deploy_key.pub >> /home/deploy/.ssh/authorized_keys

# 권한 설정
sudo chown -R deploy:deploy /home/deploy/.ssh
sudo chmod 600 /home/deploy/.ssh/authorized_keys

# 임시 파일 삭제
rm ~/deploy_key.pub
```

---

### 방법 3: echo로 직접 입력 (가장 빠름) ⚡

**리눅스 서버에서 한 번에 실행:**

```bash
# SSH 디렉토리 생성
sudo mkdir -p /home/deploy/.ssh
sudo chmod 700 /home/deploy/.ssh

# 공개키를 직접 입력 (윈도우에서 확인한 내용으로 변경)
echo "ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIFwbll+D//Sc6TaC+cLPX7PfOA59iDDed/8f9FJpold6 deploy@server" | sudo tee -a /home/deploy/.ssh/authorized_keys

# 권한 설정
sudo chown -R deploy:deploy /home/deploy/.ssh
sudo chmod 600 /home/deploy/.ssh/authorized_keys
```

**⚠️ 주의:** `echo "..."` 부분의 공개키 내용을 윈도우에서 확인한 실제 내용으로 변경하세요!

---

## 🔍 공개키 내용 확인 방법

### 윈도우 PowerShell에서:

```powershell
cd C:\FTP
Get-Content deploy_key.pub
```

**예상 출력:**
```
ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIFwbll+D//Sc6TaC+cLPX7PfOA59iDDed/8f9FJpold6 deploy@server
```

**이 전체 내용을 복사하세요!**

---

## 📋 추천 방법: nano로 직접 입력

### Step 1: 윈도우에서 공개키 확인

```powershell
cd C:\FTP
Get-Content deploy_key.pub
```

공개키 내용을 화면에 표시된 대로 복사하거나 메모해두세요.

### Step 2: 리눅스 서버에서 파일 생성

```bash
sudo mkdir -p /home/deploy/.ssh
sudo chmod 700 /home/deploy/.ssh
sudo nano /home/deploy/.ssh/authorized_keys
```

### Step 3: nano에서 입력

**nano 에디터가 열리면:**

1. **VMware 클립보드 사용** (작동하는 경우):
   - 윈도우에서 공개키 복사
   - 리눅스 nano에서 마우스 우클릭 → Paste

2. **직접 타이핑** (가장 확실):
   - 윈도우 화면을 보면서 공개키 내용을 타이핑
   - 한 줄이므로 금방 입력 가능

3. **저장**:
   - Ctrl+X
   - Y
   - Enter

### Step 4: 권한 설정

```bash
sudo chown -R deploy:deploy /home/deploy/.ssh
sudo chmod 600 /home/deploy/.ssh/authorized_keys
```

---

## ✅ 등록 확인

### 1. 파일 확인

```bash
cat /home/deploy/.ssh/authorized_keys
```

**예상 결과:**
```
ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIFwbll+D//Sc6TaC+cLPX7PfOA59iDDed/8f9FJpold6 deploy@server
```

### 2. 권한 확인

```bash
ls -la /home/deploy/.ssh/
```

**예상 결과:**
```
total 12
drwx------ 2 deploy deploy 4096 Nov 28 13:50 .
drwxr-xr-x 4 deploy deploy 4096 Nov 28 13:45 ..
-rw------- 1 deploy deploy   XXX Nov 28 13:50 authorized_keys
```

---

## 🎯 지금 바로 할 일

### 1. 윈도우에서 공개키 확인

**PowerShell에서:**

```powershell
cd C:\FTP
Get-Content deploy_key.pub
```

### 2. 리눅스 서버에서 등록

**리눅스 서버에서:**

```bash
sudo mkdir -p /home/deploy/.ssh
sudo chmod 700 /home/deploy/.ssh
sudo nano /home/deploy/.ssh/authorized_keys
```

**nano에서:**
- 공개키 내용 입력 (복사한 내용 붙여넣기 또는 타이핑)
- Ctrl+X → Y → Enter

```bash
sudo chown -R deploy:deploy /home/deploy/.ssh
sudo chmod 600 /home/deploy/.ssh/authorized_keys
```

---

## 💡 팁

### 공개키는 한 줄입니다!

공개키는 매우 짧고 한 줄이므로:
- 직접 타이핑해도 금방 완료
- 윈도우 화면을 보면서 입력하면 됩니다

### VMware 클립보드

클립보드 공유가 작동하지 않으면:
- 직접 타이핑하는 것이 가장 확실합니다
- 공개키는 길어도 한 줄이므로 어렵지 않습니다

---

**지금 윈도우에서 `Get-Content deploy_key.pub`를 실행해서 공개키 내용을 확인하세요!**  
**그 다음 리눅스 서버에서 nano로 직접 입력하면 됩니다!** 🔑
