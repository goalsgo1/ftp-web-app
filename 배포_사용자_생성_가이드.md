# 👤 배포 사용자 생성 완료하기

현재 `sudo adduser deploy` 명령어를 실행 중입니다. 다음 단계를 따라하세요.

---

## 📋 현재 상태

화면을 보니:
- ✅ `deploy` 사용자 생성 중
- ✅ 비밀번호 설정 완료
- ⏭️ 추가 정보 입력 대기 중

---

## ✅ 다음 단계

### 1. 추가 정보 입력 (선택 사항)

현재 프롬프트:
```
Full Name []:
```

**작업:**
- **Enter** 키만 누르면 됩니다 (기본값으로 진행)
- 또는 원하는 이름 입력 후 Enter

### 2. 계속되는 프롬프트들

다음과 같은 질문들이 나올 수 있습니다:

```
Room Number []:        ← Enter
Work Phone []:         ← Enter
Home Phone []:         ← Enter
Other []:              ← Enter
```

**모두 Enter 키만 누르면 됩니다!**

### 3. 최종 확인

마지막에 다음 질문이 나옵니다:

```
Is the information correct? [Y/n]
```

**Y를 입력하고 Enter** (또는 그냥 Enter - 기본값이 Y)

---

## 📝 전체 과정 요약

```
Full Name []:                    ← Enter
Room Number []:                  ← Enter
Work Phone []:                   ← Enter
Home Phone []:                   ← Enter
Other []:                        ← Enter
Is the information correct? [Y/n] ← Enter (또는 Y 입력)
```

**모든 질문에 Enter만 누르면 됩니다!**

---

## ✅ 사용자 생성 완료 후

사용자가 성공적으로 생성되면 다음 명령어를 실행하세요:

### 1. sudo 권한 추가

```bash
sudo usermod -aG sudo deploy
```

### 2. 사용자 확인

```bash
# deploy 사용자 확인
id deploy

# sudo 그룹에 추가되었는지 확인
groups deploy
```

**예상 결과:**
```
deploy : deploy sudo
```

---

## 🎯 다음 단계

사용자 생성이 완료되면:

### 1. 배포 디렉토리 생성

```bash
sudo mkdir -p /var/www/myapp/{releases,shared,repo}
sudo chown -R deploy:deploy /var/www/myapp
```

### 2. Git 설치 및 저장소 클론

```bash
sudo apt update && sudo apt install git -y
sudo -u deploy git clone https://github.com/goalsgo1/ftp-web-app.git /var/www/myapp/repo
```

### 3. SSH 공개키 등록

```bash
sudo mkdir -p /home/deploy/.ssh
sudo chmod 700 /home/deploy/.ssh
sudo nano /home/deploy/.ssh/authorized_keys
# 공개키 붙여넣기 (Ctrl+Shift+V)
sudo chown -R deploy:deploy /home/deploy/.ssh
sudo chmod 600 /home/deploy/.ssh/authorized_keys
```

---

## 💡 팁

### 추가 정보를 입력하지 않아도 됩니다

- 모든 추가 정보는 선택 사항입니다
- Enter만 눌러서 건너뛰어도 됩니다
- 나중에 수정할 수 있습니다

### 비밀번호는 안전하게 설정하세요

- 이미 비밀번호를 설정했으므로 괜찮습니다
- 나중에 SSH 키로 로그인하면 비밀번호 없이도 가능합니다

---

## ✅ 체크리스트

- [ ] Full Name 입력 (또는 Enter)
- [ ] Room Number 입력 (또는 Enter)
- [ ] Work Phone 입력 (또는 Enter)
- [ ] Home Phone 입력 (또는 Enter)
- [ ] Other 입력 (또는 Enter)
- [ ] "Is the information correct?" → Y (또는 Enter)
- [ ] 사용자 생성 완료 확인
- [ ] sudo 권한 추가 (`sudo usermod -aG sudo deploy`)

---

**지금 모든 질문에 Enter만 누르고, 마지막에 Y를 입력하세요!** 🚀
