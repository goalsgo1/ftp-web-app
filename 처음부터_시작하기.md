# 🚀 처음부터 시작하기 - Git 저장소 만들기

아직 Git 저장소를 만들지 않았으니 처음부터 단계별로 진행하겠습니다.

---

## 📋 진행 순서

1. ✅ **로컬 Git 저장소 초기화** (`git init`)
2. ✅ **파일 추가 및 첫 커밋**
3. ✅ **GitHub에서 원격 저장소 생성**
4. ✅ **로컬과 원격 저장소 연결**
5. ✅ **코드 푸시**

---

## 1단계: 로컬 Git 저장소 초기화

PowerShell에서 다음 명령어를 실행하세요:

```powershell
# 현재 디렉토리 확인
cd C:\FTP

# Git 저장소 초기화
git init

# 기본 브랜치를 main으로 설정
git branch -M main

# 상태 확인
git status
```

**예상 결과:**
```
Initialized empty Git repository in C:/FTP/.git/
On branch main

No commits yet
...
```

---

## 2단계: Git 사용자 정보 설정 (아직 설정하지 않은 경우)

```powershell
# 사용자 이름 설정
git config --global user.name "Your Name"

# 이메일 설정 (GitHub 계정 이메일 사용 권장)
git config --global user.email "your.email@example.com"

# 확인
git config --global user.name
git config --global user.email
```

---

## 3단계: 파일 정리 (필요시)

파일들이 `public` 디렉토리에 있지 않다면 이동:

```powershell
# public 디렉토리가 없으면 생성
if (-not (Test-Path "public")) {
    New-Item -ItemType Directory -Force -Path "public\css", "public\js", "public\images"
}

# 파일 이동 (아직 안 했다면)
if (Test-Path "index.html" -and -not (Test-Path "public\index.html")) {
    Move-Item index.html public\index.html -Force
}
if (Test-Path "world_time.html" -and -not (Test-Path "public\world_time.html")) {
    Move-Item world_time.html public\world_time.html -Force
}
if (Test-Path "disk_info.php" -and -not (Test-Path "public\disk_info.php")) {
    Move-Item disk_info.php public\disk_info.php -Force
}
if (Test-Path "gallery.php" -and -not (Test-Path "public\gallery.php")) {
    Move-Item gallery.php public\gallery.php -Force
}
```

---

## 4단계: 첫 번째 커밋 생성

```powershell
# 모든 파일 추가
git add .

# 커밋 생성
git commit -m "Initial commit: FTP 웹 애플리케이션 프로젝트

- 메인 페이지 (index.html)
- 세계 시간 페이지 (world_time.html)
- 디스크 정보 페이지 (disk_info.php)
- 갤러리 페이지 (gallery.php)
- 배포 스크립트 및 CI/CD 설정"

# 상태 확인
git status
```

---

## 5단계: GitHub에서 원격 저장소 생성

1. **GitHub 웹사이트 접속**
   - https://github.com 접속
   - 로그인

2. **새 저장소 생성**
   - 우측 상단 `+` 버튼 클릭
   - `New repository` 선택

3. **저장소 정보 입력**
   - **Repository name**: 원하는 이름 입력 (예: `ftp-web-app`)
   - **Description**: 설명 입력 (선택사항)
   - **Public** 또는 **Private** 선택
   - ⚠️ **중요**: `Add a README file`, `Add .gitignore`, `Choose a license` 체크박스는 **모두 해제** (로컬에 이미 파일이 있으므로)

4. **`Create repository` 버튼 클릭**

5. **저장소 URL 복사**
   - 생성된 페이지에서 HTTPS URL 복사
   - 예: `https://github.com/사용자명/저장소명.git`

---

## 6단계: 원격 저장소 연결

PowerShell에서:

```powershell
# 원격 저장소 추가 (위에서 복사한 URL 사용)
git remote add origin https://github.com/사용자명/저장소명.git

# 원격 저장소 확인
git remote -v

# 결과 예시:
# origin  https://github.com/사용자명/저장소명.git (fetch)
# origin  https://github.com/사용자명/저장소명.git (push)
```

---

## 7단계: 코드 푸시 (GitHub에 업로드)

```powershell
# 첫 번째 푸시
git push -u origin main
```

**인증이 필요한 경우:**
- GitHub에서 Personal Access Token 생성 필요
- 비밀번호 대신 토큰 입력

---

## 🔐 GitHub 인증 방법

### 방법 1: Personal Access Token (HTTPS)

1. **GitHub에서 토큰 생성**
   - GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
   - `Generate new token (classic)` 클릭
   - 권한 선택: `repo` (전체 선택)
   - 토큰 생성 후 복사

2. **푸시 시 사용**
   - Username: GitHub 사용자명
   - Password: 생성한 토큰 입력

### 방법 2: SSH 키 (권장)

1. **SSH 키 생성**
   ```powershell
   ssh-keygen -t ed25519 -C "your.email@example.com"
   # Enter 두 번 (비밀번호 없이)
   ```

2. **공개키 복사**
   ```powershell
   Get-Content ~\.ssh\id_ed25519.pub | clip
   ```

3. **GitHub에 등록**
   - GitHub → Settings → SSH and GPG keys → New SSH key
   - 복사한 공개키 붙여넣기

4. **SSH URL로 변경**
   ```powershell
   git remote set-url origin git@github.com:사용자명/저장소명.git
   ```

---

## ✅ 완료 확인

```powershell
# 원격 저장소 정보 확인
git remote -v

# 브랜치 정보 확인
git branch -a

# 최근 커밋 확인
git log --oneline
```

---

## 📚 참고 문서

- **빠른 시작**: `빠른_시작_가이드.md`
- **상세 가이드**: `단계별_배포_가이드.md`
- **Git 정보 확인**: `Git_정보_확인.md`

---

## 🆘 문제 해결

### `git init` 오류
- 이미 `.git` 디렉토리가 있는지 확인: `Test-Path .git`
- 있다면 삭제 후 재시도: `Remove-Item -Recurse -Force .git`

### `git push` 인증 실패
- Personal Access Token 사용 확인
- 또는 SSH 키 설정 확인

### 파일이 추가되지 않음
- `.gitignore` 파일 확인
- `git status`로 상태 확인

---

**1단계부터 차근차근 진행하세요!** 🚀
