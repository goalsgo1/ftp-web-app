# Feature Templates Firestore 인덱스

## 📋 개요

`featureTemplates` 컬렉션에서 사용하는 Firestore 인덱스 설정 가이드입니다.

---

## 🔍 현재 구현 상태

현재는 **복합 인덱스 없이도 작동하도록** 구현되어 있습니다:

- `order` 필드로만 쿼리 (복합 인덱스 불필요)
- 클라이언트에서 `name`으로 2차 정렬

---

## ⚙️ 복합 인덱스 (선택사항)

더 효율적인 쿼리를 원하는 경우, 다음 복합 인덱스를 생성할 수 있습니다:

### 인덱스 정보

**컬렉션**: `featureTemplates`

**필드**:
1. `order` (Ascending)
2. `name` (Ascending)

### 생성 방법

#### 방법 1: 에러 메시지 링크 사용

Firebase 에러 메시지에 제공된 링크를 클릭하면 자동으로 인덱스 생성 페이지로 이동합니다.

#### 방법 2: Firebase 콘솔에서 수동 생성

1. [Firebase Console](https://console.firebase.google.com) 접속
2. 프로젝트 선택
3. **Firestore Database** → **인덱스** 탭
4. **복합 인덱스 만들기** 클릭
5. 다음 정보 입력:
   - 컬렉션 ID: `featureTemplates`
   - 필드 추가:
     - `order` (오름차순)
     - `name` (오름차순)
6. **만들기** 클릭

---

## 📝 인덱스 생성 후 코드 변경 (선택사항)

인덱스를 생성했다면, 쿼리를 다음과 같이 변경할 수 있습니다 (더 효율적):

```typescript
// 현재 (복합 인덱스 불필요)
const q = query(
  collection(db, 'featureTemplates'),
  orderBy('order', 'asc')
);
// 클라이언트에서 name으로 정렬

// 인덱스 생성 후 (더 효율적)
const q = query(
  collection(db, 'featureTemplates'),
  orderBy('order', 'asc'),
  orderBy('name', 'asc')
);
// DB에서 정렬 완료
```

**현재 코드는 복합 인덱스 없이도 작동하므로 변경할 필요는 없습니다.**

---

## ✅ 현재 상태

- ✅ **복합 인덱스 생성 완료**
- ✅ DB에서 `order`, `name` 필드로 정렬 (효율적)
- ✅ 인덱스 사용으로 빠른 쿼리 성능
- ✅ 코드 최적화 완료

---

**작성일**: 2025-01-27

