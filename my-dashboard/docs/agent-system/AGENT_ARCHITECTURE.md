# 🤖 에이전트 시스템 아키텍처

## 📋 개요

Claude API를 활용한 24시간 자동화 에이전트 시스템 설계 문서입니다.

## 🎯 핵심 전략

### 토큰 사용량 최적화

```
작업 분류 기준:
├── Claude Code (Cursor) 사용
│   ├── 간단한 코드 작성/수정
│   ├── 버그 수정
│   ├── 리팩토링
│   └── 문서 작성
│
└── Claude API 호출 (서브 에이전트)
    ├── 복잡한 의사결정
    ├── AI 분석 작업 (뉴스 분석, 요약)
    ├── 전략 수립 (마케팅 전략)
    ├── 콘텐츠 생성
    └── 에이전트 간 협업
```

### 비용 최적화 원칙

1. **간단한 작업 = Claude Code**: 무료 (Cursor Pro 구독)
2. **복잡한 작업 = Claude API**: 유료지만 효율적
3. **반복 작업 = 스크립트화**: 한 번 작성 후 자동 실행
4. **에이전트 체인**: 작업을 단계별로 분리하여 효율성 극대화

---

## 🏗️ 시스템 아키텍처

```
┌─────────────────────────────────────────────────────────┐
│                    Main Orchestrator                     │
│              (Claude API - Sonnet 4.5)                   │
│                                                           │
│  역할: 전체 워크플로우 관리, 에이전트 조율                │
└──────────────────┬──────────────────────────────────────┘
                   │
       ┌───────────┴───────────┐
       │                       │
┌──────▼──────┐      ┌─────────▼─────────┐
│ Data Agent  │      │ Marketing Agent   │
│             │      │                   │
│ - 스크래핑  │      │ - 콘텐츠 생성     │
│ - 분석      │      │ - 전략 수립       │
│ - 저장      │      │ - SNS 자동화      │
└──────┬──────┘      └─────────┬─────────┘
       │                       │
       │              ┌────────▼─────────┐
       │              │ Revenue Agent    │
       │              │                  │
       │              │ - 수익화 전략    │
       │              │ - 모니터링       │
       │              └──────────────────┘
       │
┌──────▼──────────────────────────────────┐
│        Firebase Firestore                │
│  (데이터 저장 및 상태 관리)              │
└──────────────────────────────────────────┘
```

---

## 🔄 에이전트 체인 (Agent Chain)

### 1. 데이터 수집 체인

```
News Scraper Agent
  ↓
Content Analyzer Agent (Claude API)
  ↓
Data Storage Agent
  ↓
Insight Generator Agent (Claude API)
```

### 2. 마케팅 체인

```
Strategy Agent (Claude API)
  ↓
Content Creator Agent (Claude API)
  ↓
Distribution Agent (SNS 자동화)
  ↓
Analytics Agent
```

### 3. 수익화 체인

```
Revenue Strategy Agent (Claude API)
  ↓
Implementation Agent
  ↓
Monitoring Agent
  ↓
Optimization Agent (Claude API)
```

---

## 📁 프로젝트 구조

```
my-dashboard/
├── app/
│   ├── api/
│   │   ├── agents/
│   │   │   ├── orchestrator/
│   │   │   │   └── route.ts          # 메인 오케스트레이터
│   │   │   ├── data-agent/
│   │   │   │   ├── route.ts          # 데이터 수집 에이전트
│   │   │   │   └── analyze.ts        # AI 분석 에이전트
│   │   │   ├── marketing-agent/
│   │   │   │   ├── route.ts          # 마케팅 에이전트
│   │   │   │   └── content-generator.ts
│   │   │   └── revenue-agent/
│   │   │       └── route.ts          # 수익화 에이전트
│   │   │
│   │   └── agent-jobs/
│   │       ├── schedule/             # 스케줄링된 작업
│   │       └── queue/                # 작업 큐
│   │
│   └── lib/
│       ├── agents/
│       │   ├── base-agent.ts         # 베이스 에이전트 클래스
│       │   ├── claude-client.ts      # Claude API 클라이언트
│       │   ├── agent-chain.ts        # 에이전트 체인 로직
│       │   └── tools/                # 에이전트 도구들
│       │       ├── file-tools.ts
│       │       ├── db-tools.ts
│       │       └── api-tools.ts
│       │
│       └── firebase/
│           └── agentJobs.ts          # 에이전트 작업 저장
│
└── scripts/
    └── agents/
        ├── run-orchestrator.ts       # 오케스트레이터 실행
        └── scheduled-tasks.ts        # 스케줄링 스크립트
```

---

## 🤖 에이전트 타입 정의

### Base Agent

```typescript
interface BaseAgent {
  id: string;
  name: string;
  role: string;
  tools: AgentTool[];
  execute(task: AgentTask): Promise<AgentResult>;
  callSubAgent(subAgentId: string, task: AgentTask): Promise<AgentResult>;
}
```

### Agent Task

```typescript
interface AgentTask {
  id: string;
  type: 'data' | 'marketing' | 'revenue' | 'analysis';
  input: Record<string, any>;
  priority: 'low' | 'medium' | 'high';
  parentTaskId?: string;  // 에이전트 체인 추적용
  subTasks?: AgentTask[]; // 하위 작업들
}
```

### Agent Result

```typescript
interface AgentResult {
  taskId: string;
  success: boolean;
  output?: Record<string, any>;
  error?: string;
  cost?: {
    tokens: number;
    price: number;
  };
  subAgentResults?: AgentResult[]; // 하위 에이전트 결과들
}
```

---

## 💰 비용 최적화 전략

### 1. 작업 분류 매트릭스

| 작업 유형 | 사용 방법 | 예상 비용 |
|---------|---------|----------|
| 코드 작성/수정 | Claude Code | 무료 |
| 간단한 데이터 처리 | Node.js 스크립트 | 무료 |
| AI 분석/요약 | Claude API (Sonnet) | $0.003/1K input |
| 전략 수립 | Claude API (Sonnet) | $0.015/1K output |
| 콘텐츠 생성 | Claude API (Haiku) | $0.00025/1K input |

### 2. 토큰 절감 전략

- **배치 처리**: 여러 뉴스를 한 번에 분석
- **캐싱**: 유사한 요청 결과 재사용
- **프롬프트 최적화**: 불필요한 컨텍스트 제거
- **모델 선택**: 간단한 작업은 Haiku, 복잡한 작업은 Sonnet

### 3. 예상 월 비용 (예시)

```
데이터 수집 에이전트:
- 뉴스 스크래핑: 무료 (스크립트)
- 뉴스 분석 (1000개/일): $0.90/일 ≈ $27/월

마케팅 에이전트:
- 콘텐츠 생성 (10개/일): $0.05/일 ≈ $1.5/월
- 전략 수립 (주 1회): $0.20/주 ≈ $0.80/월

수익화 에이전트:
- 분석 및 최적화: $0.50/일 ≈ $15/월

총 예상 비용: 약 $45/월
```

---

## 🔄 24시간 자동화 플로우

### 일일 워크플로우

```
00:00 - 데이터 수집 시작 (스크래핑)
  ↓
00:30 - AI 분석 에이전트 실행
  ↓
01:00 - 인사이트 생성 및 저장
  ↓
02:00 - 마케팅 전략 수립 (주 1회)
  ↓
08:00 - 콘텐츠 생성 및 배포
  ↓
12:00 - 수익화 분석
  ↓
18:00 - 최적화 실행
  ↓
23:00 - 일일 리포트 생성
```

---

## 🚀 구현 단계

### Phase 1: 기본 인프라 구축
- [ ] Claude API 클라이언트 구현
- [ ] Base Agent 클래스 구현
- [ ] 에이전트 작업 큐 시스템
- [ ] Firebase에 작업 저장 구조

### Phase 2: 데이터 에이전트 구현
- [ ] News Scraper Agent
- [ ] Content Analyzer Agent
- [ ] Insight Generator Agent

### Phase 3: 마케팅 에이전트 구현
- [ ] Strategy Agent
- [ ] Content Creator Agent
- [ ] Distribution Agent

### Phase 4: 수익화 에이전트 구현
- [ ] Revenue Strategy Agent
- [ ] Monitoring Agent
- [ ] Optimization Agent

### Phase 5: 오케스트레이터 구현
- [ ] Main Orchestrator
- [ ] 에이전트 체인 관리
- [ ] 스케줄링 시스템

### Phase 6: 모니터링 및 최적화
- [ ] 비용 추적
- [ ] 성능 모니터링
- [ ] 자동 최적화

---

## 📚 참고 자료

- [Anthropic Claude API 문서](https://docs.anthropic.com/)
- [Agent Pattern 설계](https://en.wikipedia.org/wiki/Software_agent)
- [Firebase Cloud Functions 스케줄링](https://firebase.google.com/docs/functions/schedule-functions)

