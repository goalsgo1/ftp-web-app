# ⚠️ 개인키 파일 확인하기

현재 `known_hosts` 파일만 보이는데, 이것은 **개인키가 아닙니다**.

---

## ❌ `known_hosts`는 개인키가 아닙니다!

`known_hosts` 파일은:
- SSH 서버의 공개키를 저장하는 파일
- 보안 확인용 (서버가 진짜 서버인지 확인)
- 개인키가 아닙니다!

---

## ✅ 찾아야 할 파일: 개인키

SSH 개인키 파일은 보통 다음 이름을 가집니다:

### 개인키 파일 이름
- `id_rsa` ← **이것을 찾아야 함!**
- `id_ed25519` ← 또는 이것
- `id_ecdsa`
- `id_dsa`

### 공개키 파일 (이것도 아님)
- `id_rsa.pub`
- `id_ed25519.pub`

---

## 🔍 .ssh 폴더에서 확인하기

파일 탐색기에서 `.ssh` 폴더를 확인하세요:

### 찾아야 할 파일:
1. **`id_rsa`** - RSA 개인키 (가장 많이 사용)
2. **`id_ed25519`** - Ed25519 개인키 (최신)

### 이 파일들은 개인키가 아님:
- ❌ `known_hosts` - 서버 공개키 저장 파일
- ❌ `id_rsa.pub` - 공개키 (서버 등록용)
- ❌ `config` - SSH 설정 파일

---

## 🔍 PowerShell에서 확인하기

PowerShell에서 실행:

```powershell
# .ssh 폴더로 이동
cd ~\.ssh

# 개인키 파일 찾기 (id_로 시작하고 .pub이 아닌 파일)
Get-ChildItem | Where-Object { $_.Name -like "id_*" -and $_.Name -notlike "*.pub" }
```

또는 간단하게:

```powershell
# 모든 id_ 파일 확인
Get-ChildItem ~\.ssh\id_*
```

---

## 🚀 개인키 파일이 없는 경우

`.ssh` 폴더에 `id_rsa`나 `id_ed25519` 파일이 없다면 **새로 생성**해야 합니다!

### 새 SSH 키 생성 (PowerShell)

```powershell
# 현재 디렉토리로 이동 (FTP 프로젝트 폴더)
cd C:\FTP

# 배포 전용 SSH 키 생성
ssh-keygen -t ed25519 -C "deploy@server" -f deploy_key

# Enter 두 번 (비밀번호 없이)
```

**생성된 파일:**
- ✅ `deploy_key` ← **이것이 개인키!**
- ✅ `deploy_key.pub` ← 공개키 (서버 등록용)

### 개인키 확인

```powershell
# 생성된 개인키 확인
Get-Content deploy_key
```

**형식:**
```
-----BEGIN OPENSSH PRIVATE KEY-----
b3BlbnNzaC1rZXktdjEAAAAABG5vbmUAAAAEbm9uZQAAAAAAAAABAAAAMwAAAAtzc2gtZW
...
-----END OPENSSH PRIVATE KEY-----
```

---

## 📋 개인키 파일 특징

### 개인키 파일의 특징:
1. **이름**: `id_rsa`, `id_ed25519` 등 (`id_`로 시작)
2. **확장자 없음**: `.pub`이 아님
3. **내용**: `-----BEGIN`로 시작
4. **크기**: 보통 수백 바이트 ~ 수 KB

### 개인키 파일 내용 예시:

```
-----BEGIN OPENSSH PRIVATE KEY-----
b3BlbnNzaC1rZXktdjEAAAAABG5vbmUAAAAEbm9uZQAAAAAAAAABAAAAMwAAAAtzc2gtZW
QyNTUxOQAAACBxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
...
-----END OPENSSH PRIVATE KEY-----
```

---

## ✅ 다음 단계

### 방법 1: 기존 개인키가 있는 경우

1. `.ssh` 폴더에서 `id_rsa` 또는 `id_ed25519` 파일 찾기
2. 파일 열기 (메모장 또는 텍스트 에디터)
3. 전체 내용 복사
4. GitHub Secret의 `DEPLOY_KEY`에 붙여넣기

### 방법 2: 개인키가 없는 경우

1. 새 SSH 키 생성 (위의 명령어 실행)
2. `deploy_key` 파일 내용 복사
3. GitHub Secret의 `DEPLOY_KEY`에 붙여넣기

---

## 🔍 파일 탐색기에서 확인하는 방법

1. `.ssh` 폴더 열기 (이미 열려있음)
2. 파일 목록 확인
3. **`id_rsa`** 또는 **`id_ed25519`** 파일 찾기
4. 없으면 → 새로 생성 필요

---

## 💡 빠른 확인

`.ssh` 폴더에서:
- ✅ `id_rsa` 파일이 보이나요? → 이것이 개인키!
- ✅ `id_ed25519` 파일이 보이나요? → 이것도 개인키!
- ❌ `known_hosts`만 보이나요? → 개인키가 없음, 새로 생성 필요

---

**지금 `.ssh` 폴더에서 `id_rsa` 또는 `id_ed25519` 파일이 보이나요?**  
**없으면 위의 명령어로 새로 생성하세요!** 🔑
