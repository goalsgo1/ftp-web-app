# 📊 현재 진행 상황 및 다음 단계

지금까지 진행된 내용과 다음에 해야 할 일을 정리했습니다.

---

## ✅ 완료된 작업

### 1. 로컬 Git 저장소 설정 ✅
- [x] Git 저장소 초기화 (`git init`)
- [x] 브랜치 설정 (`main`)
- [x] 첫 번째 커밋 생성 (26개 파일, 6976줄)
- [x] Git 사용자 정보 설정

### 2. GitHub 저장소 연결 ✅
- [x] GitHub 저장소 생성 (`goalsgo1/ftp-web-app`)
- [x] 원격 저장소 연결 (`git remote add origin`)
- [x] 코드 푸시 완료 (`git push -u origin main`)
- [x] 브랜치 추적 설정 완료

### 3. GitHub 인증 설정 ✅
- [x] Personal Access Token 생성
- [x] GitHub 인증 완료

### 4. GitHub Secrets 설정 (진행 중) ⚠️
- [x] `DEPLOY_KEY` 추가 완료 (SSH 개인키)
- [ ] `DEPLOY_HOST` 추가 필요 (서버 IP 주소)
- [ ] `DEPLOY_USER` 추가 필요 (배포 사용자명)

### 5. 프로젝트 구조 ✅
- [x] 디렉토리 구조 생성 (`public/`, `deploy/`, `.github/`)
- [x] 파일 정리 완료
- [x] 배포 스크립트 생성 (`deploy/deploy.sh`)
- [x] GitHub Actions 워크플로우 생성 (`.github/workflows/deploy.yml`)

---

## ⏭️ 다음 할 일 (우선순위 순)

### 🔥 긴급 (지금 바로)

#### 1. GitHub Secrets 완료하기

**아직 추가하지 않은 Secret:**

1. **DEPLOY_HOST 추가**
   - GitHub → Settings → Secrets and variables → Actions
   - "New secret" 클릭
   - Name: `DEPLOY_HOST`
   - Secret: 서버 IP 주소
     - 예: `192.168.1.100`
     - 예: `172.30.1.42`
   - "Add secret" 클릭

2. **DEPLOY_USER 추가**
   - "New secret" 클릭
   - Name: `DEPLOY_USER`
   - Secret: `deploy` (또는 배포 사용자명)
   - "Add secret" 클릭

**확인:**
- Settings → Secrets and variables → Actions
- 다음 3개가 모두 보여야 함:
  - ✅ DEPLOY_HOST
  - ✅ DEPLOY_USER
  - ✅ DEPLOY_KEY

---

### 📋 중요 (서버 준비)

#### 2. 리눅스 서버 준비

**서버에서 실행할 작업:**

1. **배포 사용자 생성**
   ```bash
   sudo adduser deploy
   sudo usermod -aG sudo deploy
   ```

2. **배포 디렉토리 생성**
   ```bash
   sudo mkdir -p /var/www/myapp/{releases,shared,repo}
   sudo chown -R deploy:deploy /var/www/myapp
   ```

3. **Git 설치 및 저장소 클론**
   ```bash
   sudo apt update && sudo apt install git -y
   sudo -u deploy git clone https://github.com/goalsgo1/ftp-web-app.git /var/www/myapp/repo
   ```

4. **SSH 공개키 등록**
   - Windows에서 공개키 복사:
     ```powershell
     Get-Content deploy_key.pub | clip
     ```
   - 서버에서:
     ```bash
     sudo mkdir -p /home/deploy/.ssh
     sudo chmod 700 /home/deploy/.ssh
     sudo nano /home/deploy/.ssh/authorized_keys
     # 공개키 붙여넣기
     sudo chown -R deploy:deploy /home/deploy/.ssh
     sudo chmod 600 /home/deploy/.ssh/authorized_keys
     ```

5. **배포 스크립트 설정**
   ```bash
   sudo cp /var/www/myapp/repo/deploy/deploy.sh /var/www/myapp/
   sudo chmod +x /var/www/myapp/deploy.sh
   sudo chown deploy:deploy /var/www/myapp/deploy.sh
   ```

6. **Nginx 설정 수정**
   ```bash
   sudo nano /etc/nginx/sites-available/default
   ```
   - `root /var/www/myapp/current/public;` 로 변경
   ```bash
   sudo nginx -t
   sudo systemctl reload nginx
   ```

7. **초기 심볼릭 링크 생성**
   ```bash
   sudo -u deploy ln -s /var/www/myapp/repo/public /var/www/myapp/current
   ```

---

### 🧪 테스트 (서버 준비 후)

#### 3. 자동 배포 테스트

1. **작은 변경사항 추가**
   ```powershell
   # 파일 수정 (예: index.html 제목 변경)
   git add .
   git commit -m "Test: 자동 배포 확인"
   git push origin main
   ```

2. **GitHub Actions 확인**
   - https://github.com/goalsgo1/ftp-web-app/actions
   - 워크플로우 실행 상태 확인
   - 배포 성공 여부 확인

3. **서버에서 확인**
   ```bash
   # 서버에 SSH 접속
   ssh -i deploy_key deploy@서버IP
   
   # 릴리즈 확인
   ls -la /var/www/myapp/releases/
   
   # 현재 링크 확인
   ls -la /var/www/myapp/current
   ```

4. **웹 브라우저에서 확인**
   - 서버 IP로 접속
   - 변경사항이 반영되었는지 확인

---

## 📊 진행률 요약

| 단계 | 상태 | 진행률 |
|------|------|--------|
| 로컬 Git 설정 | ✅ 완료 | 100% |
| GitHub 저장소 | ✅ 완료 | 100% |
| GitHub 인증 | ✅ 완료 | 100% |
| GitHub Secrets | ⚠️ 진행 중 | 33% (1/3) |
| 서버 준비 | ❌ 미완료 | 0% |
| 자동 배포 테스트 | ❌ 미완료 | 0% |

**전체 진행률: 약 60%**

---

## 🎯 오늘 할 일 체크리스트

### 지금 바로 (5분)
- [ ] `DEPLOY_HOST` Secret 추가
- [ ] `DEPLOY_USER` Secret 추가
- [ ] Secrets 목록에서 3개 모두 확인

### 서버 준비 (30분~1시간)
- [ ] 서버에 SSH 접속 가능한지 확인
- [ ] 배포 사용자 생성
- [ ] 배포 디렉토리 생성
- [ ] Git 저장소 클론
- [ ] SSH 공개키 등록
- [ ] 배포 스크립트 설정
- [ ] Nginx 설정 수정

### 테스트 (10분)
- [ ] 코드 푸시
- [ ] GitHub Actions 확인
- [ ] 웹 브라우저에서 확인

---

## 📚 참고 문서

- **전체 가이드**: `단계별_배포_가이드.md`
- **빠른 시작**: `빠른_시작_가이드.md`
- **서버 준비**: `단계별_배포_가이드.md`의 "5. 서버 준비" 섹션
- **CI/CD 설정**: `단계별_배포_가이드.md`의 "7. GitHub Actions CI/CD 설정" 섹션

---

## 🆘 문제 해결

### GitHub Secrets가 작동하지 않아요
- Secret 이름이 정확한지 확인 (대문자)
- 키가 올바르게 복사되었는지 확인

### 서버 접속이 안 돼요
- SSH 공개키가 서버에 등록되었는지 확인
- 서버 IP 주소가 맞는지 확인
- 방화벽 설정 확인

### GitHub Actions가 실패해요
- Secrets가 모두 등록되었는지 확인
- 서버에 배포 스크립트가 있는지 확인
- 서버 접속 권한 확인

---

## 💡 다음 단계 요약

**지금 바로:**
1. GitHub Secrets 완료 (`DEPLOY_HOST`, `DEPLOY_USER` 추가)

**그 다음:**
2. 서버 준비 (배포 환경 설정)

**마지막:**
3. 자동 배포 테스트

---

**지금 `DEPLOY_HOST`와 `DEPLOY_USER`를 추가하세요!** 🚀
