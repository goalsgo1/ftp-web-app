# 🚀 Git 저장소 초기화 및 설정 - 다음 단계

Git이 설치되어 있으므로 (`git version 2.52.0.windows.1`) 다음 단계를 진행하세요.

## 📋 PowerShell에서 실행할 명령어들

### 1단계: Git 사용자 정보 설정 (아직 설정하지 않은 경우)

```powershell
# 사용자 이름 설정
git config --global user.name "Your Name"

# 이메일 설정
git config --global user.email "your.email@example.com"

# 설정 확인
git config --global user.name
git config --global user.email
```

### 2단계: 프로젝트 디렉토리로 이동 (이미 C:\FTP에 있음)

```powershell
cd C:\FTP
```

### 3단계: Git 저장소 초기화

```powershell
# Git 저장소 초기화
git init

# 기본 브랜치를 main으로 설정
git branch -M main
```

### 4단계: 파일 구조 확인 및 정리

```powershell
# 현재 디렉토리 구조 확인
ls

# public 디렉토리가 있는지 확인
Test-Path public

# 파일을 public 디렉토리로 이동 (아직 이동하지 않은 경우)
if (Test-Path "index.html") { Move-Item index.html public\index.html -Force }
if (Test-Path "world_time.html") { Move-Item world_time.html public\world_time.html -Force }
if (Test-Path "disk_info.php") { Move-Item disk_info.php public\disk_info.php -Force }
if (Test-Path "gallery.php") { Move-Item gallery.php public\gallery.php -Force }
```

### 5단계: 첫 번째 커밋 생성

```powershell
# 모든 파일 추가
git add .

# 커밋 메시지와 함께 커밋
git commit -m "Initial commit: FTP 웹 애플리케이션 프로젝트

- 메인 페이지 (index.html)
- 세계 시간 페이지 (world_time.html)
- 디스크 정보 페이지 (disk_info.php)
- 갤러리 페이지 (gallery.php)
- 배포 스크립트 및 CI/CD 설정"
```

### 6단계: 상태 확인

```powershell
# Git 상태 확인
git status

# 커밋 로그 확인
git log --oneline
```

---

## 🎯 다음 단계

### GitHub 저장소 생성 및 연결

1. **GitHub에서 저장소 생성**
   - https://github.com 접속
   - 우측 상단 `+` → `New repository`
   - 저장소 이름 입력 (예: `ftp-web-app`)
   - `Create repository` 클릭

2. **로컬 저장소와 연결**
   ```powershell
   # 원격 저장소 추가 (HTTPS 방식)
   git remote add origin https://github.com/사용자명/저장소명.git
   
   # 또는 SSH 방식 (SSH 키가 설정된 경우)
   # git remote add origin git@github.com:사용자명/저장소명.git
   
   # 원격 저장소 확인
   git remote -v
   ```

3. **첫 번째 푸시**
   ```powershell
   git push -u origin main
   ```

---

## 📚 상세 가이드

- **빠른 시작**: `빠른_시작_가이드.md` 파일 참고
- **전체 가이드**: `단계별_배포_가이드.md` 파일 참고

---

## ✅ 체크리스트

- [ ] Git 사용자 정보 설정
- [ ] Git 저장소 초기화 (`git init`)
- [ ] 파일을 public 디렉토리로 이동
- [ ] 첫 번째 커밋 생성 (`git commit`)
- [ ] GitHub 저장소 생성
- [ ] 원격 저장소 연결 (`git remote add`)
- [ ] 첫 번째 푸시 (`git push`)

---

**각 단계를 순서대로 실행해보세요!**
