# ⚡ 1분 빠른 시작 (Git 이미 설치됨)

PowerShell에서 다음 명령어들을 **순서대로** 실행하세요:

## 🔥 한 번에 실행하기 (복사해서 붙여넣기)

```powershell
# 1. 디렉토리 확인
cd C:\FTP

# 2. Git 저장소 초기화
git init
git branch -M main

# 3. 파일 정리 (public 디렉토리로 이동 - 아직 안 했으면)
if (Test-Path "index.html" -and -not (Test-Path "public\index.html")) { Move-Item index.html public\index.html -Force }
if (Test-Path "world_time.html" -and -not (Test-Path "public\world_time.html")) { Move-Item world_time.html public\world_time.html -Force }
if (Test-Path "disk_info.php" -and -not (Test-Path "public\disk_info.php")) { Move-Item disk_info.php public\disk_info.php -Force }
if (Test-Path "gallery.php" -and -not (Test-Path "public\gallery.php")) { Move-Item gallery.php public\gallery.php -Force }

# 4. 모든 파일 추가
git add .

# 5. 첫 커밋
git commit -m "Initial commit"

# 6. 상태 확인
git status
```

---

## 📝 개별 실행 (단계별로)

### 1. Git 저장소 초기화
```powershell
git init
git branch -M main
```

### 2. 파일 추가 및 커밋
```powershell
git add .
git commit -m "Initial commit"
```

### 3. 상태 확인
```powershell
git status
```

---

## 🎯 다음 단계

1. **GitHub 저장소 생성**
   - https://github.com/new 접속
   - 저장소 이름 입력 후 생성

2. **연결 및 푸시**
   ```powershell
   git remote add origin https://github.com/사용자명/저장소명.git
   git push -u origin main
   ```

자세한 내용: `빠른_시작_가이드.md` 또는 `단계별_배포_가이드.md` 참고
