# ⚠️ 디렉토리 존재 오류 해결

현재 오류: `/var/www/myapp/repo` 디렉토리가 이미 존재합니다.

---

## ❌ 현재 오류

```
fatal: destination path '/var/www/myapp/repo' already exists and is not an empty directory.
```

**의미:** 대상 디렉토리가 이미 존재하고 비어있지 않음

---

## ✅ 해결 방법

### 방법 1: 기존 디렉토리 확인 후 삭제 (권장)

#### 1. 디렉토리 내용 확인

```bash
ls -la /var/www/myapp/repo
```

#### 2. 이미 클론되어 있다면

디렉토리 안에 `.git` 폴더가 있다면 이미 클론되어 있을 수 있습니다:

```bash
ls -la /var/www/myapp/repo/.git
```

**이미 클론되어 있다면:**
- 추가 작업 불필요
- 다음 단계로 진행

#### 3. 비어있거나 필요 없다면 삭제

```bash
# 디렉토리 삭제
sudo rm -rf /var/www/myapp/repo

# 다시 클론
sudo -u deploy git clone https://github.com/goalsgo1/ftp-web-app.git /var/www/myapp/repo
```

---

### 방법 2: 기존 디렉토리 내용 확인

디렉토리가 이미 클론되어 있는지 확인:

```bash
# 디렉토리 내용 확인
sudo ls -la /var/www/myapp/repo

# Git 저장소인지 확인
cd /var/www/myapp/repo
sudo -u deploy git status
```

**이미 Git 저장소라면:**
```bash
# 최신 코드로 업데이트
cd /var/www/myapp/repo
sudo -u deploy git pull origin main
```

---

## 🔍 현재 상태 확인

다음 명령어로 현재 상태를 확인하세요:

```bash
# 디렉토리 존재 여부 확인
ls -la /var/www/myapp/

# repo 디렉토리 내용 확인
ls -la /var/www/myapp/repo/

# Git 저장소인지 확인
sudo -u deploy git -C /var/www/myapp/repo status
```

---

## ✅ 권장 해결 방법

### Step 1: 디렉토리 확인

```bash
sudo ls -la /var/www/myapp/repo
```

### Step 2: 선택

**A. 이미 Git 저장소가 있다면:**
```bash
# 최신 코드로 업데이트
cd /var/www/myapp/repo
sudo -u deploy git pull origin main
```

**B. 비어있거나 필요 없다면:**
```bash
# 삭제 후 재클론
sudo rm -rf /var/www/myapp/repo
sudo -u deploy git clone https://github.com/goalsgo1/ftp-web-app.git /var/www/myapp/repo
```

---

## 🎯 다음 단계

저장소 클론이 완료되면:

### 1. 저장소 확인

```bash
# 저장소 확인
sudo ls -la /var/www/myapp/repo

# 파일 목록 확인
sudo ls -la /var/www/myapp/repo/public/
```

### 2. 배포 스크립트 설정

```bash
# 배포 스크립트 복사
sudo cp /var/www/myapp/repo/deploy/deploy.sh /var/www/myapp/
sudo chmod +x /var/www/myapp/deploy.sh
sudo chown deploy:deploy /var/www/myapp/deploy.sh
```

### 3. 초기 심볼릭 링크 생성

```bash
sudo -u deploy ln -s /var/www/myapp/repo/public /var/www/myapp/current
```

---

## 📋 체크리스트

- [ ] `/var/www/myapp/repo` 디렉토리 확인
- [ ] Git 저장소인지 확인
- [ ] 필요시 삭제 후 재클론
- [ ] 또는 이미 클론되어 있다면 다음 단계로 진행

---

**지금 `sudo ls -la /var/www/myapp/repo` 명령어로 디렉토리 내용을 확인하세요!** 🔍
