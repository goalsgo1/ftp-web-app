# 🔑 SSH 키 생성하기 - 실전 가이드

현재 개인키가 없으니 새로 생성해야 합니다. SSH 키를 생성하면 **개인키와 공개키 두 개가 함께** 생성됩니다!

---

## ✅ 확인 결과

현재 `.ssh` 폴더에는:
- ✅ `known_hosts` - 서버 목록 (이건 개인키 아님)
- ❌ `id_rsa` - 없음
- ❌ `id_ed25519` - 없음

→ **개인키가 아직 생성되지 않았습니다!**

---

## 🔑 SSH 키 생성 시 생성되는 파일

SSH 키를 생성하면 **자동으로 2개의 파일**이 생성됩니다:

### 1. 개인키 (Private Key)
- **이름**: `id_rsa` 또는 `id_ed25519`
- **용도**: GitHub Secret에 등록
- **특징**: 비밀 파일, 절대 공개하지 않음

### 2. 공개키 (Public Key)
- **이름**: `id_rsa.pub` 또는 `id_ed25519.pub` (`.pub` 확장자)
- **용도**: 서버에 등록
- **특징**: 공개해도 되는 파일

**둘 다 같은 키 쌍(Key Pair)의 일부입니다!**

---

## 🚀 SSH 키 생성하기

### 방법 1: 배포 전용 키 생성 (권장) ⭐

프로젝트 폴더에서 배포 전용 키를 생성하는 것이 좋습니다.

#### PowerShell에서 실행:

```powershell
# 현재 프로젝트 폴더에 있음 확인
cd C:\FTP

# 배포 전용 SSH 키 생성
ssh-keygen -t ed25519 -C "deploy@server" -f deploy_key
```

**실행 시 질문:**
1. **"Enter passphrase"** → **Enter** (비밀번호 없이)
2. **"Enter same passphrase again"** → **Enter** (다시 Enter)

#### 생성되는 파일:

```
C:\FTP\
├── deploy_key      ← 개인키 (GitHub Secret에 사용)
└── deploy_key.pub  ← 공개키 (서버에 등록)
```

**두 개가 함께 생성됩니다!**

---

### 방법 2: 기본 위치에 키 생성 (`.ssh` 폴더)

```powershell
# SSH 키 생성 (기본 위치: ~\.ssh\)
ssh-keygen -t ed25519 -C "your.email@example.com"
```

**실행 시 질문:**
1. **"Enter file in which to save the key"** → **Enter** (기본 위치 사용)
2. **"Enter passphrase"** → **Enter** (비밀번호 없이)
3. **"Enter same passphrase again"** → **Enter**

#### 생성되는 파일:

```
C:\Users\hmlee\.ssh\
├── id_ed25519      ← 개인키
└── id_ed25519.pub  ← 공개키
```

**역시 두 개가 함께 생성됩니다!**

---

## 📋 생성 후 확인하기

### PowerShell에서 확인:

```powershell
# 방법 1로 생성했다면 (프로젝트 폴더)
Get-ChildItem C:\FTP\deploy_key*

# 방법 2로 생성했다면 (.ssh 폴더)
Get-ChildItem ~\.ssh\id_ed25519*
```

**예상 결과:**

```
Mode                 LastWriteTime         Length Name
----                 -------------         ------ ----
-a---        2025-01-XX   XX:XX            XXX deploy_key      ← 개인키
-a---        2025-01-XX   XX:XX             XX deploy_key.pub  ← 공개키
```

---

## 🔍 개인키와 공개키 확인

### 개인키 내용 보기:

```powershell
# 배포 키인 경우
Get-Content C:\FTP\deploy_key

# 기본 키인 경우
Get-Content ~\.ssh\id_ed25519
```

**개인키 형식:**
```
-----BEGIN OPENSSH PRIVATE KEY-----
b3BlbnNzaC1rZXktdjEAAAAABG5vbmUAAAAEbm9uZQAAAAAAAAABAAAAMwAAAAtzc2gtZW
(긴 암호 문자열...)
-----END OPENSSH PRIVATE KEY-----
```

### 공개키 내용 보기:

```powershell
# 배포 키인 경우
Get-Content C:\FTP\deploy_key.pub

# 기본 키인 경우
Get-Content ~\.ssh\id_ed25519.pub
```

**공개키 형식:**
```
ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIFwbll+D//Sc6TaC+cLPX7PfOA59iDDed/8f9FJpold6 deploy@server
```

**한 줄로 짧습니다!**

---

## ✅ 생성 후 다음 단계

### 1. 개인키 복사 (GitHub Secret용)

```powershell
# 배포 키인 경우
Get-Content C:\FTP\deploy_key | clip

# 기본 키인 경우
Get-Content ~\.ssh\id_ed25519 | clip
```

### 2. GitHub Secret에 등록

- Name: `DEPLOY_KEY`
- Secret: 복사한 개인키 전체 내용 붙여넣기

### 3. 공개키 복사 (서버 등록용 - 나중에)

```powershell
# 배포 키인 경우
Get-Content C:\FTP\deploy_key.pub | clip

# 기본 키인 경우
Get-Content ~\.ssh\id_ed25519.pub | clip
```

이 공개키는 나중에 서버에 등록해야 합니다.

---

## 📊 생성 과정 요약

```
ssh-keygen 명령어 실행
    ↓
개인키 생성
    ↓
공개키도 자동 생성 (쌍으로)
    ↓
두 개의 파일 완성!
```

**한 번에 두 개가 모두 생성됩니다!**

---

## 🎯 지금 바로 실행하기

PowerShell에서 다음 명령어를 실행하세요:

```powershell
cd C:\FTP
ssh-keygen -t ed25519 -C "deploy@server" -f deploy_key
```

**Enter 두 번 누르면:**

1. ✅ `deploy_key` (개인키) 생성됨
2. ✅ `deploy_key.pub` (공개키) 생성됨

**두 개가 함께 생성됩니다!**

---

## 💡 추천

**배포 전용 키를 만드는 것을 권장합니다:**

- ✅ 프로젝트 폴더에 있어서 관리하기 쉬움
- ✅ 배포용으로 명확함
- ✅ 기본 키와 분리되어 안전함

---

## 🔍 확인 체크리스트

- [ ] SSH 키 생성 명령어 실행
- [ ] Enter 두 번 (비밀번호 없이)
- [ ] `deploy_key` 파일 생성 확인 (개인키)
- [ ] `deploy_key.pub` 파일 생성 확인 (공개키)
- [ ] 개인키 내용 복사
- [ ] GitHub Secret에 등록

---

**지금 PowerShell에서 `ssh-keygen -t ed25519 -C "deploy@server" -f deploy_key`를 실행해보세요!** 🚀
